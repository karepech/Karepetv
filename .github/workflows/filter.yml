# .github/workflows/filter.yml
name: Filter M3U Live Events

on:
  # Jalankan secara terjadwal setiap hari pada pukul 04:00 UTC (sekitar jam 11:00 WIB)
  schedule:
    - cron: '0 4 * * *'
  # Memungkinkan eksekusi manual dari tab Actions
  workflow_dispatch:

jobs:
  filter:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout kode
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies (requests)
        run: pip install requests

      - name: Jalankan Skrip Filter
        run: python filter_m3u.py
        env:
          PYTHONUNBUFFERED: 1 

      - name: Commit dan Push file M3U hasil filter
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          
          if git diff --quiet live_events.m3u; then
            echo "Tidak ada perubahan pada live_events.m3u. Melewatkan commit."
          else
            echo "Perubahan terdeteksi. Melakukan commit..."
            git add live_events.m3u
            git commit -m "Update M3U Live Events (Automated)"
            git push
          fi
